server {
        listen 80;
        listen [::]:80;

        server_name   pilot.paraguide.uk;

        root /var/www/pilot.paraguide.uk;
        index index.html;

        location / {
                try_files $uri $uri/ =404;
        }
}

